{
  "props": [
    {
      "property": "size",
      "type": "\"S\" | \"M\" | \"L\" | \"XL\"",
      "default": "'M'",
      "description": "The size of the accordion item."
    },
    {
      "property": "density",
      "type": "\"compact\" | \"regular\" | \"spacious\"",
      "default": "'regular'",
      "description": "The amount of space between the accordion item."
    },
    {
      "property": "isQuiet",
      "type": "boolean",
      "description": "Whether the accordion item should be displayed with a quiet style."
    },
    {
      "property": "children",
      "type": "ReactNode",
      "required": true,
      "description": "The contents of the accordion item, consisting of a accordion item title and accordion item\npanel."
    },
    {
      "property": "id",
      "type": "Key",
      "description": "An id for the accordion item, matching the id used in `expandedKeys`."
    },
    {
      "property": "isDisabled",
      "type": "boolean",
      "description": "Whether the accordion item is disabled."
    },
    {
      "property": "onExpandedChange",
      "type": "(isExpanded: boolean) => void",
      "description": "Handler that is called when the accordion item's expanded state changes."
    },
    {
      "property": "isExpanded",
      "type": "boolean",
      "description": "Whether the accordion item is expanded (controlled)."
    },
    {
      "property": "defaultExpanded",
      "type": "boolean",
      "description": "Whether the accordion item is expanded by default (uncontrolled)."
    },
    {
      "property": "slot",
      "type": "string",
      "description": "A slot name for the component. Slots allow the component to receive props from a parent\ncomponent. An explicit `null` value indicates that the local props completely override all\nprops received from a parent.",
      "inheritedFrom": "SlotProps"
    },
    {
      "property": "styles",
      "type": "StylesProp",
      "description": "Spectrum-defined styles, returned by the `style()` macro.",
      "inheritedFrom": "StyleProps"
    }
  ]
}
