{
  "props": [
    {
      "property": "showDivider",
      "type": "boolean",
      "description": "Whether the column should render a divider between it and the next column."
    },
    {
      "property": "allowsResizing",
      "type": "boolean",
      "description": "Whether the column allows resizing."
    },
    {
      "property": "align",
      "type": "'start' | 'center' | 'end'",
      "default": "'start'",
      "description": "The alignment of the column's contents relative to its allotted width."
    },
    {
      "property": "children",
      "type": "ReactNode",
      "required": true,
      "description": "The content to render as the column header."
    },
    {
      "property": "menuItems",
      "type": "ReactNode",
      "description": "Menu fragment to be rendered inside the column header's menu."
    },
    {
      "property": "id",
      "type": "Key",
      "description": "The unique id of the column.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "allowsSorting",
      "type": "boolean",
      "description": "Whether the column allows sorting.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "isRowHeader",
      "type": "boolean",
      "description": "Whether a column is a [row header](https://www.w3.org/TR/wai-aria-1.1/#rowheader) and should be announced by assistive technology during row navigation.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "textValue",
      "type": "string",
      "description": "A string representation of the column's contents, used for accessibility announcements.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "width",
      "type": "ColumnSize | null",
      "description": "The width of the column. This prop only applies when the `<Table>` is wrapped in a `<ResizableTableContainer>`.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "defaultWidth",
      "type": "ColumnSize | null",
      "description": "The default width of the column. This prop only applies when the `<Table>` is wrapped in a `<ResizableTableContainer>`.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "minWidth",
      "type": "ColumnStaticSize | null",
      "description": "The minimum width of the column. This prop only applies when the `<Table>` is wrapped in a `<ResizableTableContainer>`.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "maxWidth",
      "type": "ColumnStaticSize | null",
      "description": "The maximum width of the column. This prop only applies when the `<Table>` is wrapped in a `<ResizableTableContainer>`.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "focusMode",
      "type": "'child' | 'cell'",
      "description": "Whether the column header or its first focusable child element should be focused when the column header is focused. Defaults to 'child' in arrow keyboard navigation mode and 'cell' in tab keyboard navigation mode.",
      "inheritedFrom": "ColumnProps"
    },
    {
      "property": "allowsArrowNavigation",
      "type": "boolean",
      "description": "Whether the column should support arrow key navigation even when the containing table uses tab keyboard navigation. Allows users to navigate between columns and rows with arrow keys while focus is on an interactive child element within the column header.",
      "inheritedFrom": "ColumnProps"
    }
  ]
}
