{
  "AccordionBase": [
    {
      "attribute": "allowMultiple",
      "type": "boolean",
      "description": "Reflected as `allow-multiple`. When set, multiple items may be open at the same time."
    },
    {
      "attribute": "level",
      "property": "level",
      "type": "AccordionHeadingLevel",
      "default": "3",
      "description": "Heading level applied to every item header (2–6). Defaults to 3.\nValues outside that range are clamped."
    },
    {
      "attribute": "density",
      "property": "density",
      "type": "AccordionDensity",
      "default": "'regular'",
      "description": "Controls vertical spacing between items."
    },
    {
      "attribute": "quiet",
      "property": "quiet",
      "type": "boolean",
      "default": "false",
      "description": "Renders the accordion in its quiet (no-border) visual variant."
    },
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Disables all items in the accordion. Individual items may also be\ndisabled independently."
    },
    {
      "attribute": "allow-multiple",
      "property": "allowMultiple",
      "type": "boolean",
      "default": "false",
      "description": "When set, multiple items may be open at the same time. By default only\none item can be open."
    }
  ],
  "AccordionItemBase": [
    {
      "attribute": "open",
      "property": "open",
      "type": "boolean",
      "description": "Whether the accordion item panel is expanded."
    },
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Whether the accordion item is disabled. A disabled item keeps its header\nin the tab order but blocks toggling."
    },
    {
      "attribute": "size",
      "property": "size",
      "type": "AccordionSize | undefined",
      "description": "The size of the item. Inherited from the parent accordion; controls which\nchevron icon is displayed. Has no effect when the item is used standalone."
    }
  ],
  "AssetBase": [
    {
      "attribute": "variant",
      "property": "variant",
      "type": "AssetVariant | undefined",
      "description": "The variant of the asset. When not provided, slot content is rendered (e.g., an image)."
    },
    {
      "attribute": "label",
      "property": "label",
      "type": "string",
      "default": "''",
      "description": "Accessible label for the asset’s file or folder variant."
    }
  ],
  "AvatarBase": [
    {
      "attribute": "src",
      "property": "src",
      "type": "string",
      "default": "''",
      "description": "URL of the profile image to display."
    },
    {
      "attribute": "alt",
      "property": "alt",
      "type": "string | undefined",
      "description": "Text description of the avatar image."
    },
    {
      "attribute": "size",
      "property": "size",
      "type": "AvatarSize",
      "default": "500",
      "description": "The size of the avatar. Invalid values fall back to the default (500)."
    },
    {
      "attribute": "outline",
      "property": "outline",
      "type": "boolean",
      "default": "false",
      "description": "Renders a visual outline around the avatar image."
    },
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Renders the avatar at reduced opacity, indicating the entity is inactive or unavailable."
    },
    {
      "attribute": "decorative",
      "property": "decorative",
      "type": "boolean",
      "default": "false",
      "description": "Marks the avatar as decorative, hiding it from assistive technology."
    }
  ],
  "BadgeBase": [
    {
      "attribute": "fixed",
      "property": "fixed",
      "type": "FixedValues | undefined",
      "description": "The fixed position of the badge."
    },
    {
      "attribute": "subtle",
      "property": "subtle",
      "type": "boolean",
      "default": "false",
      "description": "Whether the badge is subtle."
    },
    {
      "attribute": "outline",
      "property": "outline",
      "type": "boolean",
      "default": "false",
      "description": "Whether the badge is outlined.\n\nCan only be used with semantic variants."
    }
  ],
  "ButtonBase": [
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Whether the button is disabled. Removes focusability and prevents\ninteraction."
    },
    {
      "attribute": "accessible-label",
      "property": "accessibleLabel",
      "type": "string | undefined",
      "description": "Accessible label forwarded to the internal `<button>` element as\n`aria-label`. Required for icon-only buttons, which have no visible text."
    }
  ],
  "ButtonGroupBase": [
    {
      "attribute": "orientation",
      "property": "orientation",
      "type": "ButtonGroupOrientation",
      "default": "'horizontal'",
      "description": "The layout direction of the button group.\n\nNote: this property does NOT set `aria-orientation` on the host because\n`aria-orientation` is only meaningful for roles that manage arrow-key\nnavigation (e.g. `toolbar`, `listbox`). Since button-group uses\nsequential Tab navigation (not roving tabindex), the attribute would\nbe invalid."
    },
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Whether all buttons in the group are disabled. When `true`, forces\nevery slotted button child to `disabled`. When `false`, children\nretain their individual disabled state — a button slotted with\n`disabled` remains disabled even when the group is enabled."
    },
    {
      "attribute": "align",
      "property": "align",
      "type": "ButtonGroupAlignment",
      "default": "'start'",
      "description": "The alignment of buttons within the group along the main axis."
    }
  ],
  "ColorHandleBase": [
    {
      "attribute": "color",
      "property": "color",
      "type": "string",
      "default": "'rgba(255, 0, 0, 0.5)'",
      "description": "The CSS color value shown inside the handle. Supports any valid CSS color\nstring, including alpha transparency (which reveals the checkerboard).\n\nDefault is semi-transparent red so the opacity checkerboard is visible\nwhen the component is rendered without a `color` attribute."
    },
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Reflected. Suppresses the loupe (`open && !disabled`)."
    },
    {
      "attribute": "focused",
      "property": "focused",
      "type": "boolean",
      "default": "false",
      "description": "Reflected. Set by the parent color component when the parent picker is\nfocused (keyboard or programmatic); enlarges the handle as the focus\nindicator."
    },
    {
      "attribute": "open",
      "property": "open",
      "type": "boolean",
      "default": "false",
      "description": "Reflected. Shows the built-in loupe; auto-toggled by touch pointers."
    },
    {
      "attribute": "fill",
      "property": "fill",
      "type": "boolean",
      "default": "true",
      "description": "Reflected. `true` shows the inner color swatch (1st-gen behavior);\n`false` renders an outline-only handle."
    }
  ],
  "ColorLoupeBase": [
    {
      "attribute": "open",
      "property": "open",
      "type": "boolean",
      "default": "false",
      "description": "Whether the loupe is visible. When `false` the loupe is hidden via\nCSS opacity and transform transitions."
    },
    {
      "attribute": "color",
      "property": "color",
      "type": "string",
      "default": "'rgba(255, 0, 0, 0.5)'",
      "description": "The CSS color value to display inside the loupe.\nSupports any valid CSS color string, including those with alpha\ntransparency (which reveals the checkerboard behind).\n\nDefault is semi-transparent red so the opacity checkerboard is visible\nwhen the component is rendered without a `color` attribute."
    }
  ],
  "DividerBase": [
    {
      "attribute": "vertical",
      "property": "vertical",
      "type": "boolean",
      "default": "false",
      "description": "Whether the divider is vertical. If false, the divider is horizontal. The default is false."
    },
    {
      "attribute": "static-color",
      "property": "staticColor",
      "type": "DividerStaticColor | undefined",
      "description": "The static color variant to use for the divider."
    }
  ],
  "IconBase": [
    {
      "attribute": "label",
      "property": "label",
      "type": "string",
      "default": "''",
      "description": "Accessible label for the icon."
    }
  ],
  "IllustratedMessageBase": [
    {
      "attribute": "size",
      "property": "size",
      "type": "IllustratedMessageSize",
      "default": "'m'",
      "description": "The size of the message"
    },
    {
      "attribute": "orientation",
      "property": "orientation",
      "type": "IllustratedMessageOrientation",
      "default": "'vertical'",
      "description": "The layout orientation"
    }
  ],
  "LinearProgressMixin": [
    {
      "attribute": "value",
      "property": "value",
      "type": "number",
      "default": "0",
      "description": "Current value. Sanitized via `clampedValue` for rendering and\n`aria-valuenow`."
    },
    {
      "attribute": "min-value",
      "property": "minValue",
      "type": "number",
      "default": "0",
      "description": "Bottom of the value range."
    },
    {
      "attribute": "max-value",
      "property": "maxValue",
      "type": "number",
      "default": "100",
      "description": "Top of the value range."
    },
    {
      "attribute": "accessible-label",
      "property": "accessibleLabel",
      "type": "string",
      "default": "''",
      "description": "Rare-case accessible-name fallback used when there is no visible\n`label` slot content (for example, a data grid of meters).\nRenders into `aria-label` on the role element when set."
    },
    {
      "attribute": "value-label",
      "property": "valueLabel",
      "type": "string | undefined",
      "description": "Custom value text (e.g. `\"1 of 4\"`). Overrides the auto-formatted\nvalue in both rendered text and `aria-valuetext`."
    },
    {
      "attribute": "label-position",
      "property": "labelPosition",
      "type": "LinearProgressLabelPosition",
      "default": "'top'",
      "description": "Position of the label relative to the bar."
    },
    {
      "attribute": "static-color",
      "property": "staticColor",
      "type": "LinearProgressStaticColor | undefined",
      "description": "Static color override for use on images or colored backgrounds."
    }
  ],
  "MeterBase": [
    {
      "attribute": "variant",
      "property": "variant",
      "type": "MeterVariant",
      "default": "'informative'",
      "description": "The variant of the meter. Drives the bar fill color."
    }
  ],
  "PendingMixin": [
    {
      "attribute": "pending",
      "property": "pending",
      "type": "boolean",
      "default": "false",
      "description": "Whether the element is in a pending (busy) state. The element remains\nfocusable but activation is suppressed."
    },
    {
      "attribute": "pending-label",
      "property": "pendingLabel",
      "type": "string | undefined",
      "description": "Custom accessible label used during the pending state. When omitted, the\npending label is derived from the resolved non-busy accessible name plus\na busy suffix (e.g. \"Save, busy\")."
    }
  ],
  "PopoverBase": [
    {
      "attribute": "open",
      "property": "open",
      "type": "boolean",
      "default": "false",
      "description": "Whether the popover is open."
    },
    {
      "attribute": "modal",
      "property": "modal",
      "type": "boolean",
      "default": "false",
      "description": "Opt in to blocking modal behavior (`<dialog>.showModal()`): focus trap,\nbackground inert, native `role=\"dialog\"`. When unset, the popover uses\n`popover=\"auto\"` light-dismiss behavior."
    },
    {
      "attribute": "accessible-label",
      "property": "accessibleLabel",
      "type": "string",
      "default": "''",
      "description": "Accessible name for the popover's dialog surface, forwarded as `aria-label`\nto the internal element. Required in both modes (the surface is a dialog);\nthe component dev-warns when opened without one."
    },
    {
      "attribute": "placement",
      "property": "placement",
      "type": "Placement",
      "default": "'bottom'",
      "description": "The placement of the popover relative to its trigger."
    },
    {
      "attribute": "size",
      "property": "size",
      "type": "PopoverSize | undefined",
      "description": "Optional fixed size. When set, the popover uses a fixed inline size\n(`s` → 336px, `m` → 416px, `l` → 576px); when unset, it fits its contents."
    },
    {
      "attribute": "hide-arrow",
      "property": "hideArrow",
      "type": "boolean",
      "default": "false",
      "description": "Hide the popover's arrow (tip). The arrow is shown by default."
    },
    {
      "attribute": "offset",
      "property": "offset",
      "type": "number",
      "default": "8",
      "description": "Main-axis offset in pixels from the trigger."
    },
    {
      "attribute": "cross-offset",
      "property": "crossOffset",
      "type": "number",
      "default": "0",
      "description": "Cross-axis offset in pixels from the trigger."
    },
    {
      "attribute": "should-flip",
      "property": "shouldFlip",
      "type": "boolean",
      "default": "true",
      "description": "Allow the popover to flip to the opposite side when constrained. When\n`false`, the popover stays in the requested placement."
    },
    {
      "attribute": "for",
      "property": "for",
      "type": "string | undefined",
      "description": "ID of the trigger element in the same document tree root."
    },
    {
      "attribute": "manual",
      "property": "manual",
      "type": "boolean",
      "default": "false",
      "description": "Suppress the automatic click-to-toggle wiring on the resolved trigger. When\nset, control visibility through the `open` property instead. ARIA\nrelationship wiring still applies."
    }
  ],
  "ProgressBarBase": [
    {
      "attribute": "indeterminate",
      "property": "indeterminate",
      "type": "boolean",
      "default": "false",
      "description": "When true, the bar runs a looping fill animation and all four\n`aria-value*` attributes are omitted from the DOM. The visible\nvalue text is also omitted."
    }
  ],
  "ProgressCircleBase": [
    {
      "attribute": "label",
      "property": "label",
      "type": "string",
      "default": "''",
      "description": "Accessible label for the progress circle.\n\nUsed to provide context about what is loading or progressing.\nWhen no accessible name is provided (no label, aria-label, or\naria-labelledby), a default \"Loading\" label is applied."
    },
    {
      "attribute": "progress",
      "property": "progress",
      "type": "number | null",
      "default": "null",
      "description": "Progress value from 0 to 100.\n\nWhen `null` (indeterminate), the component shows a loading animation.\nSetting a number switches to determinate mode. Removing the `progress`\nattribute or setting this property to `null` returns to indeterminate.\nValues outside 0–100 or non-finite numbers are clamped (non-finite becomes 0).\n\nReflected to the `progress` attribute when set; the attribute is omitted when indeterminate."
    }
  ],
  "SizedMixin": [
    {
      "attribute": "size",
      "property": "size",
      "type": "ElementSize"
    }
  ],
  "TabBase": [
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Whether this tab is disabled. Disabled tabs are announced as\nunavailable by assistive technology and cannot be activated,\nbut remain focusable via arrow keys within the tablist."
    },
    {
      "attribute": "selected",
      "property": "selected",
      "type": "boolean",
      "default": "false",
      "description": "Whether this tab is currently selected. Managed by the parent\n`swc-tabs` container — consumers should not set this directly.\nUse `swc-tabs[selected]` instead."
    },
    {
      "attribute": "vertical",
      "property": "vertical",
      "type": "boolean",
      "default": "false",
      "description": "Whether the tab is displayed in vertical orientation.\n\nOrientation is primarily driven by the parent `swc-tabs` `direction`\nattribute; this property remains for styling and any consumers that read\nit explicitly. We can remove it in a future major once vertical layout is\nfully parent-driven only—track that cleanup in the component backlog if\nit becomes redundant in practice."
    },
    {
      "attribute": "tab-id",
      "property": "tabId",
      "type": "string",
      "default": "''",
      "description": "Unique identifier for this tab. Used by the parent `swc-tabs`\nto match this tab to its corresponding `swc-tab-panel`."
    }
  ],
  "TabPanelBase": [
    {
      "attribute": "selected",
      "property": "selected",
      "type": "boolean",
      "default": "false",
      "description": "Whether this panel is currently visible (its associated tab is\nselected). Managed by the parent `swc-tabs` — consumers should\nnot set this directly."
    },
    {
      "attribute": "tab-id",
      "property": "tabId",
      "type": "string",
      "default": "''",
      "description": "Unique identifier linking this panel to its corresponding tab.\nMust match the `tab-id` attribute on the associated `swc-tab`."
    }
  ],
  "TabsBase": [
    {
      "attribute": "keyboard-activation",
      "property": "keyboardActivation",
      "type": "KeyboardActivation",
      "description": "Whether selection follows keyboard focus (`automatic`, default) or the\nuser must press Enter or Space to activate (`manual`). Prefer `manual`\nwhen tab panels are expensive to render or not fully present in the DOM."
    },
    {
      "attribute": "density",
      "property": "density",
      "type": "TabDensity",
      "description": "Layout density: `regular` (default) or `compact` (reduced tab spacing)."
    },
    {
      "attribute": "direction",
      "property": "direction",
      "type": "TabsDirection",
      "default": "'horizontal'",
      "description": "The layout direction of the tab list: `horizontal` (default) or `vertical`."
    },
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "Whether the entire tab list is disabled. When `true`,\n`aria-disabled=\"true\"` is applied to the tablist element and\nall interaction is suppressed."
    },
    {
      "attribute": "accessible-label",
      "property": "accessibleLabel",
      "type": "string",
      "default": "''",
      "description": "Accessible label for the tablist. Rendered as `aria-label` on the\nelement with `role=\"tablist\"` in the concrete template."
    },
    {
      "attribute": "selected",
      "property": "selected",
      "type": "string",
      "default": "''",
      "description": "The `tab-id` of the currently selected tab. Setting this property\nupdates which tab appears selected and which panel is visible."
    }
  ],
  "TooltipBase": [
    {
      "attribute": "variant",
      "property": "variant",
      "type": "TooltipVariant",
      "default": "'neutral'",
      "description": "The semantic variant of the tooltip."
    },
    {
      "attribute": "placement",
      "property": "placement",
      "type": "TooltipPlacement",
      "default": "'top'",
      "description": "Preferred placement of the tooltip relative to its trigger. This is always\nthe consumer's requested side; the resolved physical side (after any\nviewport-driven flip) is reflected as `actual-placement`."
    },
    {
      "attribute": "open",
      "property": "open",
      "type": "boolean",
      "default": "false",
      "description": "Whether the tooltip is visible."
    },
    {
      "attribute": "for",
      "property": "for",
      "type": "string | undefined",
      "description": "The `id` of the trigger element in the same document tree root."
    },
    {
      "attribute": "delay",
      "property": "delay",
      "type": "number",
      "default": "1500",
      "description": "Warm-up delay in milliseconds before the tooltip opens on pointer hover.\nSet to `0` to open immediately. Keyboard focus always opens immediately."
    },
    {
      "attribute": "disabled",
      "property": "disabled",
      "type": "boolean",
      "default": "false",
      "description": "When set, the tooltip does not respond to hover or focus events and cannot\nbe opened. `disabled` takes priority over `open`: setting `open` to `true`\nwhile disabled is a no-op, and disabling an open tooltip closes it. Applies\nin `manual` mode as well."
    },
    {
      "attribute": "manual",
      "property": "manual",
      "type": "boolean",
      "default": "false",
      "description": "Suppresses automatic hover and focus wiring.\nThe consumer manages visibility via the `open` property or the popover API."
    },
    {
      "attribute": "offset",
      "property": "offset",
      "type": "number",
      "default": "4",
      "description": "Pixel gap along the placement axis between the trigger and the tooltip bubble."
    },
    {
      "attribute": "cross-offset",
      "property": "crossOffset",
      "type": "number",
      "default": "0",
      "description": "Slide along the trigger edge perpendicular to the placement direction, in pixels."
    },
    {
      "attribute": "container-padding",
      "property": "containerPadding",
      "type": "number",
      "default": "12",
      "description": "Minimum inset from the viewport edge, in pixels, for collision detection."
    },
    {
      "attribute": "should-flip",
      "property": "shouldFlip",
      "type": "boolean",
      "default": "true",
      "description": "Whether the tooltip may reposition to the opposite side when the requested\nplacement does not fit within the viewport."
    },
    {
      "attribute": "labeling",
      "property": "labeling",
      "type": "boolean",
      "default": "false",
      "description": "When set, the tooltip acts as the trigger's accessible name rather than its description.\nUse for icon-only triggers where the tooltip text is the sole accessible name."
    }
  ]
}
