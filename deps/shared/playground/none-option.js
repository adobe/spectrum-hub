// The "unset" choice for an optional property — one whose attribute may be absent
// entirely (Badge's `fixed?`, Button's `staticColor?`). Leads its option list, and
// the apply path removes the attribute rather than reflecting this string.
export const NONE_OPTION = 'None';
