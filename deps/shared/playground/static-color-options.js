// Unlike variant/fillStyle/size, staticColor has no documented default of its
// own, so its control needs an explicit "unset" sentinel the same way icon has NO_ICON.
export const NO_STATIC_COLOR = 'None';
