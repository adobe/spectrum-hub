# RSP Playground Bug Batch — Root-Cause Fixes

**Status: Tasks 4/5/6 committed; Tasks 3/7/8 closed by user decision; Task 9 Phase 1 (spike) done with zero regressions, Phase 2 (real cutover) in progress** (2026-08-13). Task 1 (the `LabelableProps` extends fix) plus a durability shim in `discover-components.js` were built, verified working end-to-end, then **reverted back to a clean tree** — the shim would have been thrown away as soon as Task 9 (the compiler rewrite) landed, and the user decided not to spend time on throwaway scaffolding. Reconsidering scope at that point surfaced that **Task 9 only fixes Class A** (~9 of the 25 reports) — it touches `extract-base-props.js`/`extract-props.js` only, so it does nothing for Class B/C/D (~16 of the 25). Decision at the time: **fix Class B/C/D first (Tasks 4–8), Task 9 last.**

Tasks 4 (`68ed120`), 6 (`5f0d194`), and 5 (`9e07cd5`) are now committed on their respective branches. Tasks 3, 7, and 8 were then **closed without code changes** on user review: Radio is never used standalone in practice (RadioGroup is), Swatch/SwatchGroup aren't actually missing (real components under different React names, already aliased), and Badge's outline-color limitation plus Help text's loading issue are expected behavior / a content-authoring fix respectively, not code bugs. **The user has asked to move to Task 9 as soon as possible** — it's the only item left in this batch (besides the deferred Task 1).

This plan classifies 25 reported RSP-playground bugs by actual root cause (verified against the code and, for the console-warning question, against a live browser reproduction), then lays out fixes. It does **not** treat "we never scanned the upstream package for inherited properties" as the explanation for all 25 — that hypothesis holds for about a third of them; the rest are distinct bugs with distinct fixes. Forcing everything into one bucket would under-fix the real inheritance gap (see [[rsp-base-props-inheritance-gap]]) while leaving several unrelated defects unaddressed.

> **For agentic workers:** Use `.ai/skills/test-driven-development/SKILL.md` for Tasks 1–6 (all are testable Node/browser logic). Task 9 (long-term compiler rewrite) should be spiked on its own branch per the existing recommendation in memory before committing to the full swap — do it last, not first, since it only covers Class A.
>
> **On shims:** Task 1 (Class A, `LabelableProps`) is deliberately sequenced last among the "fast" tasks. Don't re-implement it — or any other durability workaround for the auto-generated `components.json`/`discover-components.js` pipeline — without checking in first. The daily `extract-rsp-properties.yml` cron will silently revert any hand-edit to `components.json` that discovery can't itself re-derive; anything patching around that is throwaway scaffolding once Task 9 lands, and the user has already declined to spend time on that once.

## Root-cause classes

- **A — Missing inherited-prop resolution.** Same mechanism as the previously-fixed `isDisabled` gap: `deps/rsp/components.json`'s `extends`/`includes` entry for the component omits an interface that actually declares the missing prop, and `deps/rsp/extract-props.js` only resolves one hop (never transitive), so the prop never reaches the component's `deps/rsp/data/<Name>.json`.
- **B — Data is correct; the playground UI/apply logic is broken.** The prop **is** present in the generated JSON; the bug is in `blocks/playground/` or `deps/rsp/playground/`.
- **C — Component never added to the catalog at all.** No `components.json` entry, no `.d.ts` scan, nothing to extract.
- **D — Other.** CSS/visual, upstream-library behavior, or a composition/context requirement — not a prop-extraction issue.

## Classification table

| Item | Class | Evidence | Fix |
|---|---|---|---|
| Number field (label) | A | `components.json` NumberField extends `[NumberFieldProps, StyleProps, InputProps]` — no `LabelableProps`; `label` is declared on `LabelableProps` in `rsp-base-props.json` | Add `LabelableProps` to `extends` |
| Picker (label) | A | Same gap — extends omits `LabelableProps` | Add `LabelableProps` to `extends` |
| Progress Circle (label) | A | Same gap | Add `LabelableProps` to `extends` |
| Radio group (label) | A | `RadioGroup.json`'s own `children` prop means "the Radios inside it," not text — group has no real `label` at all; `radio-group.jsx`'s own comment confirms react-aria's RadioGroup requires an accessible name | Add `LabelableProps` to `extends` |
| Search field (label) | A | Same gap | Add `LabelableProps` to `extends` |
| Text area (label) | A | `TextArea` entry (`includes: [TextFieldProps]`) has no `LabelableProps` either | Add `LabelableProps` to `extends` |
| Text Field (label) | A | Same gap | Add `LabelableProps` to `extends` |
| Tag group (label) | A | `TagGroup.json` has no `label`; extends omits `LabelableProps` | Add `LabelableProps` to `extends` |
| Slider field (label) | A (via `includes`, not `extends`) | `Slider` entry is `{"includes": ["SliderBaseProps"]}` with no `extends` key at all; `SliderBaseProps` isn't in `rsp-base-props.json` either; `slider.jsx`'s own comment documents this gap | Add `"extends": ["LabelableProps"]` — `includes` and `extends` are two different non-transitive code paths in `extract-props.js`, neither reaches `LabelableProps` today |
| **Radio button (label)** | **D, not A** | Radio's own `children` **is** its label (`Radio.json`: `"children": "The label for the element"`, same pattern as `Checkbox.json`) — it needs no `LabelableProps` fix at all. The real bug: a standalone `<Radio>` (no `RadioGroup` ancestor) crashes the entire preview before any control can do anything — see console-warnings section below | Fix the crash (Task 3), not the extends list |
| Tag group (isEmphasized) | D (was B) | Clickability fixed (missing `selectionMode`, same as Tree view); with a confirmed-selected tag, `isEmphasized` still reaches `createElement` correctly but produces zero computed-style difference | Selection fixed; the style difference itself needs upstream S2 confirmation, not more repo-side debugging (Task 5, done) |
| AvatarGroup (missing size) | B | `AvatarGroup.json` **has** `size` (type `"16 \| 20 \| 24 \| ... \| (number & {})"`); `blocks/playground/playground-data.js` `parsePickerOptions` (`/'([^']+)'/g`) only matches quoted string literals, so this unquoted numeric union parses to zero options and `resolveControl` drops any zero-option, non-freeform property | Extend `parsePickerOptions` to also parse unquoted numeric-literal unions (Task 4) |
| Badge (outline colors) | D (unclear) | `Badge.json` has full color/variant option lists; no fillStyle-aware special-casing found anywhere in the playground code | Likely an upstream `@react-spectrum/s2` limitation (outline styling not implemented for every color), not a bug in this repo — spike/confirm before spending more time (Task 8) |
| Close button (staticColor invisible) | D | `blocks/playground/preview-shell.css` has background-contrast rules for `[static-color="black"]`/`[static-color="white"]` but none for `[static-color="auto"]` | Add an `auto` background rule (Task 6) |
| Help text (not loading) | C | Not a standalone RSP export — `deps/rsp/rsp-secondary-status.json` notes `"Use Form component"`; selecting it 404s the export lookup and hits the caught-error path | Remove from the playground catalog, or repoint at Form's `description`/`errorMessage` props (Task 8) |
| Side navigation (wrong snippet) | B/C boundary | `SideNav` **is** in `components.json` with real data; there's no `side-nav.jsx` in `deps/rsp/playground/snippets/` | Author the missing snippet (Task 6) |
| Tree view (highlight/checkbox) | B, fixed | `TreeView.json` has `selectionStyle` present; root cause was missing `selectionMode="multiple"` (RAC's `Tree` defaults to `'none'`, so neither value had anything to render) | Fixed — hardcoded `selectionMode="multiple"` in `deps/rsp/playground/snippets/tree-view.jsx` (Task 5, done) |
| Avatar (no props) | B | `Avatar.json` has 5 real props | Not extraction — see "no props" cluster below |
| Calendar (no props) | B | `Calendar.json` has real props (confirmed) | Same cluster |
| Cards (no props) | B | `Card.json` has 11 real props; `playground.js`'s `resolveComponentMeta` does `pascalCase(component)` with **no alias table**, so slug `cards` → `Cards` ≠ `Card.json` | Add a slug alias/rename (Task 4) |
| Select box (no props) | B | `SelectBox.json` has real props; slug `select-box` → `SelectBox` already matches the filename correctly | Not a slug mismatch — likely the external "components" spreadsheet's `properties` column is empty for this row (can't verify from this repo; see below) |
| Standard dialog (no props) | B | `Dialog.json` has 5 real props; slug `standard-dialog` → `StandardDialog` ≠ `Dialog.json` | Add a slug alias/rename (Task 4) |
| Table (no props) | B, slug fixed / spreadsheet still empty | Slug mismatch fixed in Task 4 (`table` now resolves to `TableView` everywhere). Live preview confirmed fully working (Task 5's `selectionMode` fix too). **Controls panel still empty on the real site** — mocked the external spreadsheet response locally with real `properties` for `table` and got 3 correct controls, proving the code path works; the live zero-controls symptom is the external spreadsheet's `table` row, same as Avatar/SelectBox/Tag | Not fixable from this repo — flag to the spreadsheet owner (Task 4 + 5, done) |
| Tag (missing) | B | `Tag.json` is a real, non-empty file; `Tag` has a `components.json` entry | Contradicts "missing" — same external-spreadsheet-row suspicion as Select box/Avatar |
| Swatch (no props) | C | No `Swatch` key in `components.json`, no `Swatch.json`, no snippet | Never added to the catalog (Task 7 territory — needs upstream `.d.ts` scan + a new entry) |
| Swatch group (no props) | C | Same — nothing anywhere for `SwatchGroup` | Same as Swatch |

**On the "no props" cluster (Avatar, Select box, Tag):** these three have correct extraction data and correct slug→filename resolution, so the "no controls shown" symptom must come from `blocks/playground/playground-data.js`'s `getComponentProperties()`, which looks up the block's authored `component` name in an **external AEM spreadsheet** (`playground-data.json?sheet=components`) and returns `[]` if that row's `properties` column is empty or the row doesn't exist. That spreadsheet isn't part of this repo, so it can't be verified or fixed from here — **flag to whoever owns the playground content spreadsheet** rather than treating it as a code bug. Cards/Standard dialog/Table are different: those slugs never resolve to the right JSON filename at all, which is a code bug (Task 4).

## Console-warning investigation (per the 2026-08-13 follow-up ask)

Tested empirically, not guessed: built a minimal harness driving `deps/rsp/playground/index.html` exactly the way `blocks/playground/playground.js` does (same postMessage contract), against the **live production esm.sh CDN build** the real playground uses, for `text-field`, `number-field`, `slider`, `avatar-group`, `tag-group`, and `tree-view` — the components with a Class A ("label" missing) or B (control drops silently) bug.

**Result: none of them produced any console warning or error.** The missing/incorrect prop is silently absent from the rendered output — no dev-mode invariant warning fires. This appears to be because `@react-spectrum/s2`'s esm.sh `?bundle` output is a production build with React/React Aria's dev warnings stripped. **This means fixing the Class A inheritance gap will not, on its own, resolve console warnings** — that hypothesis doesn't hold for this cluster. Worth double-checking against whatever specific warning text you're actually seeing in a real browser session, since it may point at something this harness doesn't reproduce (e.g. a warning that only fires with React's dev build, which the production playground doesn't load).

**What the same testing did surface, unprompted:** rendering a standalone `<Radio>` (exactly what `deps/rsp/playground/snippets/radio.jsx` renders — `<Radio>Option</Radio>`, no `RadioGroup` ancestor) throws a real, uncaught console **error**:

```
Uncaught TypeError: Cannot read properties of null (reading 'isDisabled')
```

This comes from react-aria's `useRadio` reading `RadioGroupState` off a `null` context, because Radio requires a `RadioGroup` ancestor to supply shared selection state. `radio.jsx` has an existing dev comment claiming this used to degrade silently ("renders NOTHING at all — no DOM, no console error") — so upstream `@react-spectrum/s2`/`react-aria-components` has apparently changed behavior since that comment was written, from a silent no-op to a hard crash. Since the playground's `initRsp()` always fetches whichever version esm.sh currently resolves as "latest" for the package (`https://esm.sh/${PKG}@${pkg.version}`, itself read live from `package.json`), this can drift again without warning. This is **unrelated to the LabelableProps inheritance gap** — it's a snippet-composition bug, and it's the actual explanation for "Radio button — label prop doesn't change" (the whole preview crashes before any control could do anything).

## Design decisions already made (do not re-litigate)

- **Two fix tracks, not one.** Task 1 (extends-list additions) is a narrow, low-risk patch for the ~9 confirmed Class A items. Task 7 (TS-compiler rewrite, [[rsp-base-props-inheritance-gap]]'s recommended Path B2) is the actual holistic fix — it prevents this whole class of bug for every future prop, not just `LabelableProps` — but it's a bigger effort and should be spiked separately before committing to the swap. Do Task 1 now; don't block it on Task 7.
- **Don't "fix" Radio by adding LabelableProps.** Radio's label is genuinely `children`, not an inherited `label` prop — confirmed against `Radio.json` and `Checkbox.json`'s identical pattern. The fix is the crash (Task 3).
- **The external playground-content spreadsheet is out of scope for this repo.** Select box, Avatar, and Tag's "no props" reports most likely trace to that spreadsheet's authored `properties` column, not to any file in this repo — don't spend engineering time here; report it to whoever maintains that sheet.
- **Badge and CloseButton (Class D rows) are lower-confidence.** Time-box a short spike (Task 8) rather than assuming they're extraction bugs.

## Priority order (revised 2026-08-13, then again 2026-08-13 after closing 3/7/8)

1. ✅ **Task 4** — component-slug → data-filename mismatches. Committed `68ed120`.
2. ✅ **Task 6** — small fixes (AvatarGroup size, CloseButton `staticColor="auto"`, side-nav snippet). Committed `5f0d194`.
3. ✅ **Task 5** — TreeView/TagGroup/Table `selectionMode`. Committed `9e07cd5`.
4. ⛔ **Task 3** (standalone-Radio crash) — won't fix; Radio is never used standalone in practice, RadioGroup is the realistic case.
5. ⛔ **Task 7** (Swatch/SwatchGroup catalog gap) — closed, not actually a gap: they're real components under different React names (`ColorSwatch`/`ColorSwatchPicker`), already aliased.
6. ⛔ **Task 8** (Help text, Badge outline colors) — closed; both are expected behavior / a content-authoring fix, not code bugs.
7. **→ Task 9 next, as soon as possible** (per explicit user priority) — the TS-compiler rewrite. Task 1 (`LabelableProps`) is deferred until after Task 9 lands, since Task 9 replaces the mechanism Task 1 would otherwise patch.

## Tasks

### Task 1 — Add missing `LabelableProps` extends entries (Class A cluster)
**Files:** `deps/rsp/components.json`
Add `"LabelableProps"` to the `extends` array for: `NumberField`, `Picker`, `ProgressCircle`, `RadioGroup`, `SearchField`, `TextArea`, `TextField`, `TagGroup`. For `Slider` (currently `includes`-only, no `extends` key), add `"extends": ["LabelableProps"]` alongside the existing `includes`.
Regenerate the affected `deps/rsp/data/*.json` files via the existing extraction script and confirm each now has a `label` entry (`inheritedFrom: "LabelableProps"`).
Add/extend `test/extractions/extract-props.node.test.js` coverage so a future accidental removal of one of these entries fails a test, not just a playground bug report.

### Task 2 — Do NOT touch Radio's extends list
No code change here — this task exists only to record that Radio was investigated and correctly excluded from Task 1 (see Design decisions).

### Task 3 — Fix the standalone-Radio crash — WON'T FIX (2026-08-13, user decision)
Radio is never used standalone in real usage — RadioGroup (which already works) is the realistic case. Not worth wrapping a standalone Radio in a synthetic `<RadioGroup>` just to avoid a crash nobody hits in practice. Closed, no code change.

### Task 4 — Fix component-slug → data-filename mismatches — DONE, committed (2026-08-13, branch `slug-mismatches`, commit 68ed120)
**Files:** `deps/rsp/playground/pascal-case.js`, `blocks/playground/playground.js`, `deps/rsp/playground/index.html`, `test/blocks/playground.test.js`

Added `resolveRspComponentName()` to `pascal-case.js` — a small hand-maintained `RSP_NAME_ALIASES` map (`cards`→`Card`, `standard-dialog`→`Dialog`, `table`→`TableView`) that falls back to plain `pascalCase()` for everything else. **Deliberately not** `deps/impl-aliases.js` (auto-generated by `deps/build-status-index.js`, "do not edit by hand" — same durability trap as `components.json`) — and not even the same mapping: that file solves "which RSP *docs page* covers this slug" (e.g. `select-box`→`SelectBoxGroup`, the family's page), which is wrong for a live single-component preview (which needs `select-box`→`SelectBox`, already correct with no alias). This is its own small, purpose-specific, hand-committed list.

Applied everywhere `pascalCase(component)` previously stood in for the real export name:
- `resolveComponentMeta()`'s `componentTitle` (drives the `deps/rsp/data/<Name>.json` fetch and the code-disclosure tag name).
- `deps/rsp/playground/index.html`'s `exportName` (drives the live esm.sh import and its own data fetch).

**Found and fixed two more instances of the same bug while verifying live**, not originally scoped in this task:
1. RSP snippet files are named after the real export's kebab slug, not the authored block slug (`card.jsx`/`dialog.jsx`/`table-view.jsx` exist; `cards.jsx`/`standard-dialog.jsx`/`table.jsx` don't) — `markupUrl` was still built from the raw slug. Fixed by kebab-casing the resolved name (`toSlug(componentTitle)`, reusing `scripts/utils/component-path.js`'s existing helper — verified the pascalCase↔toSlug round-trip against all 102 existing snippet filenames, zero mismatches).
2. `OVERLAY_TRIGGERS`/`overlayShape` (both in `index.html` and in `buildRspSnippet()`) were keyed by the raw slug too — `standard-dialog` has no entry (only `dialog` does), so the Dialog preview rendered nothing at all (no wrapping `DialogTrigger`+`Button`). Same fix: look these up, and the `VIRTUALIZED_RSP_COMPONENTS`/`FULL_WIDTH_RSP_COMPONENTS`/`VERTICAL_ORIENTATION_COMPONENTS` sizing Sets alongside them, by a `routeSlug` derived from the resolved name.

Verified live against the real esm.sh build (throwaway harness, deleted after use, same pattern as the console-warning investigation): `table` now renders a real `TableView` with sample rows, `cards` renders a real `Card`, `standard-dialog` renders a working "Open dialog" trigger that opens to show real dialog content. Re-verified `tooltip` (an existing, already-correct overlay route) still works — no regression. Added unit tests to `test/blocks/playground.test.js` for `resolveRspComponentName` and `resolveComponentMeta` (couldn't run them through the project's `wtr` runner — a pre-existing, unrelated `puppeteer-core` ESM/CommonJS break in this environment — verified the underlying logic directly in the browser and via `npx eslint` instead). `npm run test:extractions` unaffected (same 3 pre-existing unrelated failures as before this change).

Committed to `slug-mismatches` (68ed120) — the user cut a dedicated branch for this work.

### Task 5 — Investigate why present-but-inert controls don't reach the live preview — DONE, committed (2026-08-13, branch `misc-playground`, commit `9e07cd5`)

The two items in this task turned out to have **different root causes**, one fixable in-repo, one not.

**Tree view `selectionStyle` — fixed.** The apply-path is fine: `currentProps.selectionStyle` was reaching the live `TreeView` correctly. The real problem is upstream of that — RAC's `Tree` defaults `selectionMode` to `'none'`, and with no selection enabled, `selectionStyle` (`'highlight'` vs `'checkbox'`) has nothing to render either way. Confirmed live: with the snippet as originally authored, sending `selectionStyle="checkbox"` produced no `<input type="checkbox">` and no `aria-multiselectable`; adding `selectionMode="multiple"` to the snippet's `<TreeView>` root (the same "hardcode a prop the playground can't control yet" pattern used elsewhere, e.g. `radio-group.jsx`'s `value`) made both values render distinctly and correctly. Fixed in `deps/rsp/playground/snippets/tree-view.jsx`.

**Tag group `isEmphasized` — split into two findings after a user follow-up.** The user correctly pointed out `isEmphasized` only matters visually for *selected* tags, and that individual tags weren't clickable at all in the real playground — same root cause as Tree view: `deps/rsp/playground/snippets/tag-group.jsx`'s `<TagGroup>` had no `selectionMode`, so RAC's `TagList` defaulted it to `'none'` and tags were focusable but never actually selectable. **Fixed** by hardcoding `selectionMode="single"` on the snippet root (same pattern as `tree-view.jsx`; confirmed live that real `pointerdown`/`pointerup` sequences now toggle `aria-selected` — a bare synthetic `.click()` turned out to be an unreliable way to trigger react-aria's press handling for this specific grid/selectable-row interaction, which cost some back-and-forth during verification; pointer events are the reliable check).

With selection now genuinely working, isEmphasized was retested on a **confirmed-selected** tag and still showed **zero computed-style difference** — same `background-color`/`color`/`border-color`/`box-shadow`/`outline`/`padding`, checked on the row and every descendant, selected or not, emphasized or not. This part is not a wiring bug in this repo (the boolean reaches `createElement` correctly) and not fixable by any snippet change — reclassified Class B → Class D, needs upstream confirmation (e.g. the live S2 docs/Storybook) before spending more time on it.

**Table view — same `selectionMode` fix, requested directly by the user and applied to `table-view.jsx`** (RAC's `Table` has the identical `'none'`-by-default behavior as `Tree`/`TagList`). Cleaned up the user's own edit (extra whitespace) and added the same explanatory comment. Verified live: `aria-multiselectable="true"` now present, rows still render correctly. Separately, the user reported still seeing **zero controls at all** for Table even after that fix — traced this via a full pipeline test (mocked the external `playground-data.json` spreadsheet response with a `table` row + `isQuiet,density,selectionStyle` properties, using the real `blocks/playground/playground.js` `init()`) and got 3 real controls (switch + 2 pickers) rendering correctly. This proves the *code* is correct end-to-end post-Task-4 — the zero-controls symptom on the real site is the external AEM spreadsheet's `table` row missing/empty `properties`, the same class of gap already flagged for Avatar/SelectBox/Tag's "no props" reports. Not fixable from this repo; flag to whoever owns that spreadsheet.

`npm run test:extractions` unaffected (same 3 pre-existing unrelated failures).

### Task 6 — Small fixes — DONE (2026-08-13, branch `misc-playground`)
- **AvatarGroup size control** (`blocks/playground/playground-data.js`, `parsePickerOptions`): now also matches unquoted numeric-literal unions (split on `|`, filter to bare-digit parts), so `"16 | 20 | ... | (number & {})"` yields the 14 literal sizes and drops the open-ended `(number & {})` tail. Quoted string unions still take priority and short-circuit before the numeric branch runs. Verified live: sending `size="112"` (a string, same as the real picker sends) renders real 7rem avatars — no extra numeric coercion needed anywhere in the pipeline. Added 3 unit tests to `test/extractions/playground-data.node.test.js`.
- **CloseButton staticColor=auto** (`blocks/playground/preview-shell.css`): added a background rule for `[static-color="auto"]`, same as `black`'s. Root cause confirmed live: `auto` defaults its icon to black regardless of theme (no ambient container to auto-detect against in this bare preview shell) — forcing the browser to dark mode reproduced true black-on-near-black invisibility; the new rule fixes it. Also empirically confirmed `black` was never actually broken (icon renders solid black on the correctly-forced white background) — the original bug report's "Black" half doesn't reproduce.
- **Side navigation snippet**: authored `deps/rsp/playground/snippets/side-nav.jsx` — confirmed via `SideNav.d.ts` (fetched live from unpkg) that `SideNav` is Tree-based (same RAC `Tree` primitive as `TreeView`) and needs the same `aria-label` treatment; a two-level nested `SideNavItem`/`SideNavItemContent` snippet renders correctly (verified live — "Get started"/"Components" visible, no console errors). Note: the original report's "renders a Button placeholder" symptom did **not** reproduce with no snippet present (a clean empty mount, not a Button) — that half may be a stale observation or an external content-authoring issue outside this repo; not chased further since the concrete, addressable half (missing snippet) is now fixed.

`npm run test:extractions` unaffected (same 3 pre-existing unrelated failures). `npx eslint` clean on all changed files (CRLF noise aside, per [[crlf-line-endings]]); `stylelint` itself is broken in this environment (unrelated Node/`@csstools` ESM incompatibility) so `preview-shell.css` wasn't lint-checked that way — visually/functionally verified live instead.

### Task 7 — Add Swatch / SwatchGroup to the catalog — CLOSED, not a Class C bug (2026-08-13, user correction)
These aren't missing from the catalog at all — they're real components already, just named differently in React (`swatch` → `ColorSwatch`, `swatch-group` → `ColorSwatchPicker`; already aliased). This was a misclassification in the original investigation. No code change needed here.

### Task 8 — Spike: Help text, Badge outline colors — CLOSED (2026-08-13, user decision)
- **Badge outline colors**: confirmed by the user to be documented, expected S2 behavior, not a bug. No code change.
- **Help text**: confirmed not a standalone RSP export; the fix is authoring it differently (content-side), not a code change here.

### Task 9 (parallel, longer-term) — TS-compiler rewrite of the extraction pipeline — Phase 1 (spike) DONE, uncommitted (2026-08-13, branch `rsp-compiler-spike`)

Full design in `.claude/plans/robust-chasing-lighthouse.md` (approved plan). **Phase 1 (prove the approach works) is complete and passed with zero regressions.**

**Built:** `deps/rsp/cdn-resolve.js` (pure module-specifier → CDN URL resolution) and `deps/rsp/ts-cdn-host.js` (async import-graph crawl into a `Map<canonicalPath, source>`, then a fully synchronous `ts.CompilerHost` built from that cache → real `ts.Program`/`TypeChecker`). Added `typescript` as a new devDependency.

**Two non-obvious blockers found and fixed during the spike, both worth remembering:**
1. **`typescript@latest` (7.x) is a different, incompatible package now.** As of this session, npm's `typescript` `latest` tag points at Microsoft's new native/Go-ported compiler, whose default export is just version metadata — `ts.createProgram`/`createCompilerHost`/the whole classic API (2244 exports) are gone from the top-level import. The classic JS Compiler API this whole rewrite depends on is still published, just under the `5.x` line (`^5.9.3`, pinned — caret keeps it off the 6.x/7.x native line). Confirmed by installing 7.0.2 first and finding `Object.keys(ts).length === 2`.
2. **`Omit<T, K>` (and other mapped/utility types) silently collapse to zero properties if `T` has ANY unresolved heritage member anywhere in its chain** — not just the unresolved piece, the *entire* result. Diagnosed by bisecting real fetched `.d.ts` content down to a 1-file repro: wrapping RAC's `ButtonProps` (itself fine standalone) in even a trivial `Omit<RACButtonProps, 'className'>` produced zero properties, because RAC's `ButtonProps` itself references `react-aria/useButton`'s `AriaButtonProps` (a genuinely different package, `react-aria`, not `react-aria-components`) and RAC's `RenderProps` generic constrains on `React.JSX.IntrinsicElements` (needs real `react` types). Fixed by adding `react-aria` and `@types/react` (DefinitelyTyped, CDN-fetched like everything else — not a real npm dependency) to the crawlable package set, and fixing `react-aria-components`'s bare-subpath resolution to go through its real `package.json` "exports" map (`dist/types/exports/*`, a thin re-export layer) instead of guessing at its internal `dist/types/src/` layout 1:1 (verified: `react-aria-components/slots` has no `slots.d.ts` under `src/` at all — the subpath name and real internal filename aren't always the same).

**Phase 1 validation result** (5 components: `TextField`, `Button`, `ActionButton`, `Checkbox`, `Dialog`), diffed against today's committed `deps/rsp/data/*.json`:
- **Zero regressions** — every prop present in the old output is still present in the new output, for all 5.
- **The exact target bugs are fixed automatically, with no manual entry**: `isDisabled` now appears on `Button`/`ActionButton`; `label`/`labelPosition`/`labelAlign`/`isRequired`/`contextualHelp` now appear on `TextField`.
- Prop counts jumped up across the board (TextField 5→60, Button 7→44, ActionButton 6→43, Checkbox 5→27, Dialog 5→14) — mostly legitimate DOM/ARIA passthrough props (`aria-*`, `onKeyDown`, `onFocus`, etc.) that a real per-component filtering pass (Phase 2 scope decision, not a bug) may want to trim for table/playground display, same spirit as today's existing `EXCLUDED_PROPERTIES = new Set(['className'])`.
- One minor, non-blocking leftover: `@types/react/user-context.d.ts` 404s consistently (1 of ~260 crawled files) — doesn't affect any of the 5 components' correctness, not chased further this pass.

Added 25 unit tests (`test/extractions/cdn-resolve.node.test.js`, `test/extractions/ts-cdn-host.node.test.js`), including the offline 3-file cross-file `extends`-chain fixture that proves the core mechanism without any network access. `npm run test:extractions` passes throughout (same 3 pre-existing unrelated failures).

**Phase 2 (the real cutover) — DONE locally, uncommitted (2026-08-13, branch `rsp-compiler-spike`).** Not run via the GitHub Action and not committed to main, per explicit instruction — everything below was run locally.

- Rewrote `discover-components.js` (dropped all `extends`/`includes` derivation — no longer needed) and `extract-props.js` (now uses `ts-cdn-host.js`'s `crawl`/`buildProgram` + `checker.getPropertiesOfType()`, with a shared file cache across the whole run so the ~250 common base files are fetched once, not once per component).
- Deleted `extract-base-props.js` and `data/rsp-base-props.json` (obsolete — the checker resolves inheritance live now).
- Added two more packages to `cdn-resolve.js`'s crawl set, found by re-running the full catalog and chasing missing-prop clusters: `react-stately` and `@internationalized/date` (same `Omit<>`-collapses-on-unresolved-heritage failure mode as Phase 1's `react-aria`/`@types/react` finding, just surfacing on a different component cluster — Picker/ComboBox/DatePicker/SelectBoxGroup/SideNav/ListView/TableView/Tooltip all lost their overlay-trigger and collection props until these were added).
- Ran `discover-components.js` against the live catalog: **121 components, identical roster and identical `interface`/`file` mapping to the previous output** — zero drift in what discovery finds, confirming the regex-based "which interface" step (kept unchanged per the plan's original scope) still works correctly; only the now-removed `extends`/`includes` fields differ.
- Ran `extract-props.js` against the full catalog (~8 min, shared cache): 120/121 components extracted (the 121st, `LabeledValue`, has a **pre-existing, unrelated** malformed-interface-name bug in `findComponentInterface`'s regex — confirmed identical in the previously-committed `components.json`, and `LabeledValue.json` never existed before either; not a Phase 2 regression, not fixed in this pass).
- **Diffed the full catalog against the previously-committed `data/*.json`**: 103–107/121 components gained props (the transitive-inheritance fix reaching further, same as Phase 1). 32 components showed some previously-present prop now missing — **investigated 9 of these directly against live upstream `.d.ts` source, and all 9 were confirmed to be the old regex pipeline's already-documented "`Omit<>`/`Pick<>` don't subtract" limitation** (see `README.md`'s prior "Known limitations"): e.g. `Picker`'s real type is `Pick<ListBoxProps<T>, 'items'|'dependencies'>` (2 keys) but the old pipeline's header-token `extends` resolution can't tell a `Pick`/`Omit` from a full `extends` and pulled in *all* of `ListBoxProps`/`PopoverProps`, over-including `selectionBehavior`/`dragAndDropHooks`/`trigger`/`triggerRef`/etc.; `CloseButton` similarly over-included `isPending` despite S2's real type only picking `isDisabled`/`onPress`; `Calendar` over-included `visibleDuration` despite S2 explicitly `Omit`-ing it; same pattern confirmed for `AssetCard`'s `density`, `Slider`'s `children`, `ToastContainer`'s `queue`/`children`, and `AccordionItem`'s `isFocusVisibleWithin`/`state` (accidentally pulled in from a `RenderProps<>` render-state type, not a real authorable prop — exactly the trap a prior memory note already warned about). **None of the 32 are real regressions — the new pipeline is more correct on all 9 directly checked, fixing a second, previously-accepted bug class for free.** Also found a smaller, purely cosmetic fix: quoted string-literal property keys (e.g. `'aria-label'`) now render as the clean unquoted `aria-label` (the checker's `symbol.name` has no quote characters; the old regex captured the source text including its quotes) — a display improvement for the live `blocks/table/table.js` UI, not a data change.
- Updated `deps/rsp/README.md` (new architecture, new `components.json` schema, new "Known limitations" — including a note that `components.json` no longer survives hand-edits any better than before, and the `typescript`-version-pin / `Omit<>`-collapse findings) and `.github/workflows/extract-rsp-properties.yml` (dropped the "Extract base shared properties" step, **added a required `npm ci` step** — the new pipeline depends on the `typescript` package where the old one depended on nothing, so this would have failed in CI without it). **Neither the workflow nor `workflow_dispatch` has been run** — edited only, per instruction.
- `npm run test:extractions` passes throughout (same 3 pre-existing unrelated failures). Rewrote `test/extractions/{discover-components,extract-props}.node.test.js` for the new implementations; deleted `test/extractions/extract-base-props.node.test.js` (file under test no longer exists).

**Not yet committed** — full diff is ~134 files (mostly regenerated `data/*.json`). Presented to the user for review before committing, given the scale and the fact that this changes what a batch of ~30 components' live prop tables actually show (fewer over-included rows) alongside the ~100 components gaining previously-invisible rows.
